server:
  port: 9001

spring:
  application:
    name: nacos-order-service
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        #配置Nacos注册中心地址
        server-addr: localhost:8848

      config:
        server-addr: localhost:8848
        file-extension: yaml